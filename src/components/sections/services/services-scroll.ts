/** ScrollTrigger start for title and description. */
export const SERVICES_ENTER_START = "top 82%";

/** ScrollTrigger start — row top reaches lower viewport edge. */
export const SERVICES_ROW_ENTER_START = "top 92%";

/** Title and description fade duration. */
export const SERVICES_HEADER_DURATION = 0.32;

/** Service card and CTA fade duration. */
export const SERVICES_ITEM_DURATION = 0.38;

/** Bottom divider left-to-right grow duration. */
export const SERVICES_DIVIDER_DURATION = 0.55;

/** Divider grow ease. */
export const SERVICES_DIVIDER_EASE = "power2.inOut";

/** ScrollTrigger start — divider top reaches lower viewport edge. */
export const SERVICES_DIVIDER_ENTER_START = "top 92%";

/** Header text lift on enter (px). */
export const SERVICES_REVEAL_Y = 14;

/** Card lift on enter (rem). */
export const SERVICES_CARD_REVEAL_Y_REM = 1;

/** Content fade ease. */
export const SERVICES_REVEAL_EASE = "power2.out";

/** Description overlaps the title fade (seconds). */
export const SERVICES_DESC_OVERLAP = 0.14;

/** Each subsequent card overlap (seconds). */
export const SERVICES_CARD_CHAIN_OVERLAP = 0.22;

/** CTA overlaps the last card (seconds). */
export const SERVICES_CTA_OVERLAP = 0.14;
