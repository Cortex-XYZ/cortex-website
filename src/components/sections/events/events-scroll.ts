/** ScrollTrigger start for section title. */
export const EVENTS_ENTER_START = "top 82%";

/** ScrollTrigger start — card / follow-up top reaches lower viewport edge. */
export const EVENTS_ROW_ENTER_START = "top 92%";

/** Top rule left-to-right grow duration. */
export const EVENTS_TOP_RULE_DURATION = 0.55;

/** Top rule grow ease. */
export const EVENTS_TOP_RULE_EASE = "power2.inOut";

/** ScrollTrigger start for the xl top rule. */
export const EVENTS_TOP_RULE_ENTER_START = "top 92%";

/** Title fade duration. */
export const EVENTS_HEADER_DURATION = 0.32;

/** Event card and follow-up copy fade duration. */
export const EVENTS_ITEM_DURATION = 0.38;

/** Follow-up line fade duration. */
export const EVENTS_FOLLOW_UP_DURATION = 0.35;

/** Header text lift on enter (px). */
export const EVENTS_REVEAL_Y = 14;

/** Card lift on enter (rem). */
export const EVENTS_CARD_REVEAL_Y_REM = 1;

/** Content fade ease. */
export const EVENTS_REVEAL_EASE = "power2.out";

/** Follow-up kicker → title overlap (seconds). */
export const EVENTS_FOLLOW_UP_TITLE_OVERLAP = 0.14;

/** Follow-up title → description overlap (seconds). */
export const EVENTS_FOLLOW_UP_DESC_OVERLAP = 0.14;
