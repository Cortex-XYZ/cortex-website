/** ScrollTrigger start — matches Team / Mission entrance. */
export const MONAD_ENTER_START = "top 82%";

/** Purple divider grow duration. */
export const MONAD_DIVIDER_DURATION = 0.28;

/** Title, description, and eyebrow fade duration. */
export const MONAD_HEADER_DURATION = 0.32;

/** Topic cards and social links fade duration. */
export const MONAD_ITEM_DURATION = 0.35;

/** Text lift on enter (px). */
export const MONAD_REVEAL_Y = 14;

/** Topic card lift on enter (rem). */
export const MONAD_REVEAL_Y_REM = 1;

/** Content fade ease. */
export const MONAD_REVEAL_EASE = "power2.out";

/** Description overlaps the title fade (seconds). */
export const MONAD_DESC_OVERLAP = 0.14;

/** Band content overlaps the intro block (seconds). */
export const MONAD_BAND_OVERLAP = 0.16;

/** Social links overlap the eyebrow (seconds). */
export const MONAD_SOCIAL_OVERLAP = 0.14;

/** Social link stagger. */
export const MONAD_SOCIAL_STAGGER_EACH = 0.04;

/** ScrollTrigger start — topic row top reaches lower viewport edge. */
export const MONAD_TOPIC_ROW_ENTER_START = "top 92%";

/** Each subsequent topic card overlap within a row (seconds). */
export const MONAD_TOPIC_CHAIN_OVERLAP = 0.18;
