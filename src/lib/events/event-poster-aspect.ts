/** Canonical event poster aspect ratio. Keep in sync with `--event-cursor-preview-aspect-*` in globals.css. */
export const EVENT_POSTER_ASPECT_WIDTH = 1002;
export const EVENT_POSTER_ASPECT_HEIGHT = 774;
